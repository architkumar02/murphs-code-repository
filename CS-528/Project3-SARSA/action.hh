#ifndef __ACTION_H__
#define __ACTION_H__

class action {
    double xSpeed;
    double yawSpeed;

    public:
        action(double xSpeed, double yawSpeed);
        ~action();

    private:

};
#endif

